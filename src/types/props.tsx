import * as React from 'react';

export type props = {
    label?: string;
    children?: React.ReactNode;
    name?: string;
    type?: string;
    url?: string;
    onChange?: any;
    onClick?: any;
}

export default props;